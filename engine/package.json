{
  "name": "kickbox-sync-vps-engine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check lib/vps-sync.js && node --check lib/scrape.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "express": "^4.21.2",
    "redis": "^4.7.0"
  },
  "engines": { "node": ">=20" }
}
